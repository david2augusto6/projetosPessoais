#include <stdio.h>

int main(){

    /*int t = 1;
    while (t <=5)
        {
        int x=1;
        while (x <=10) // while (x) <=> while (x!=0)
            {
            printf("%d * %d = %d\n", t , x, t*x);
            x = x+1;
            }
        t = t + 1;
        putchar('\n');
        }*/
    
    for(int x=0;x <=10;x++){
        printf("%2d \n", x);
    }
}